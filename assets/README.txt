## AUTH

**Log In** - `POST   /v1/auth/login`

{
    emailAddress: string [required],
    password: string [required],
    rememberMe: boolean
}

**Sign Up** - `POST  /v1/auth/signup` // GETTING INTERNAL SERVER ERROR

{
    emailAddress: string [required],
    password: string [required],
    firstName: string [required],
    lastName: string [required],
    phone: string [required],
    employer: string [required]
}

**Send Recovery Email** - `POST  /v1/auth/send-password-recovery-email` // CORRECT

{
    emailAddress: string [required]
}

**Update Password and Login** - `POST  /v1/auth/update-password-and-login`

{
    password: string [required],
    token: string [required] (This is in the response from The password token that was generated by the `send-password-recovery-email` endpoint.),
}

**Log Out** - `GET  /v1/auth/logout` // NOT FOUND

{ }

**Update Password** - `PUT  /v1/auth/update-password`

{
    password: [required]
}

**Update Profile** - `PUT   /v1/auth/update-profile`

{
    emailAddress: string [required],
    firstName: string [required],
    lastName: string [required],
    phone: string [required]
}

---

## COMPANY

**Create Company** - `POST  /v1/company`

{
    name: string [required],
    phone: string [required],
    emailAddress: string [required]
}

**Get All Salespeople in Company** - `GET  /v1/company/salespeople/:id`

{
    id: string [required]
}

**Get All Customers in Company** - `GET  /v1/company/customers/:id`

{
id: string [required]
}

**Get Company By ID** - `GET  /v1/company/:id`

{
    id: string [required]
}

**Get All Companies** - `GET  /v1/company`

{ }

**Delete Company** - `DELETE /v1/company/:id`

{
    id: string [required]
}

---

## CUSTOMER

**Get Customer By ID** - `GET  /v1/customer/:id`

{
    id: string [required]
}

**Get All Customers** - `GET  /v1/customer`

{ }

**Create Customer** - `POST  /v1/customer`

{
    firstName: string [required],
    lastName: string [required],
    phone: string [required],
    emailAddress: string [required],
}

**Update Customer** - `PUT  /v1/customer`

{
    id: string [required],
    firstName: string [required],
    lastName: string [required],
    phone: string [required],
    emailAddress: string [required]
}

**Update Billing Card** - `PATCH   /v1/customer/update-billing-card`

{
    customerId: string [required],
    stripeToken: string [required],
    billingCardLastFour: string [required],
    billingCardBrand: string [required],
    billingCardExpMonth: string [required],
    billingCardExpYear: string [required]
}

**Delete Customer** - `DELETE /v1/customer/:id`

{
    id: string [required]
}

---

## WEBHOOKS

**Create Installment Plan** - `POST /v1/stripe`

{
    payload: ref [required]
}

Stripe:

Publishable Key: pk_test_zv0rLIanDWzfVBSco2aQHASv

Secret Key: sk_test_yQUMVFqMVyDKM6GNWUNs45nw

I think most of the notes so far are these:



Payment plans. There should be three options-


Single pay - $449 - twelvePayment
Three months - $150 - threePayment
12 months - $45 - singlePayment
For some reason, we're seeing 4 I believe. Is that on Stripe's end or on the app end?



Screens not working. Tried customers page and wasn't loading yet. Also, app breaks when trying to send text. Normal in simulator? Or still needs to be completed?


ACH payment doesn't go to separate form at this point (Bank account info for Routing and Checking numbers for bank)


Change "Add Spouse" to "Add Family Member / Partner"


If Family Member / Partner is clicked, the form will go through "Contact Information", then "Family Member / Partner Information", then Contact Address with a check box for "Family Member / Partner lives at the same address" checked by default. If unchecked, there will be a Family Member / Partner Address page as well
